# Elgato Stream Deck USB HID access for the openavc service user.
# Least privilege: group-owned and group-writable (0660), not world-writable —
# the openavc user is added to the plugdev group in the pi-gen configure stage.
SUBSYSTEM=="usb", ATTRS{idVendor}=="0fd9", GROUP="plugdev", MODE="0660"
